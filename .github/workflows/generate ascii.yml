
name: generate ascii
on: push

jobs:
  ascii_job:
        runs-on: ubuntu-latest

        steps:
          - name: checkout repo
            uses: actions/checkout@v6


          - name: List Repo Files
            run: ls -ltra

          - name: Executing Shell scripts
            run: | 
              ./ascii-scrpit.sh

      

